# Instructions
  1. Create an account at https://www.plaid.com
     Grab the client_id and secret_id for both sandbox and production modes

  2. Activate a python virtual enviornonment
     ```python -venv env
        source venv/bin/activate
     ```
  3. Install dependencies
     `pip install -r requirements.txt`

